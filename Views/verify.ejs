<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify Entry Code - Aisha & Mayowa Wedding</title>

  <!-- BOOTSTRAP -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <link rel="stylesheet" href="/css/styles.css" />
</head>

<body class="bg-light">

  <!-- HERO SECTION (same as index page) -->
  <div class="container py-4">
    <div class="row g-3">

      <div class="col-6 col-md-4">
        <img src="/img/couple1.jpeg" class="img-fluid rounded shadow-sm" />
      </div>

      <div class="col-6 col-md-4">
        <img src="/img/couple2.jpeg" class="img-fluid rounded shadow-sm" />
      </div>

      <div class="col-6 col-md-4">
        <img src="/img/couple3.jpeg" class="img-fluid rounded shadow-sm" />
      </div>

      <div class="col-6 col-md-4">
        <img src="/img/couple4.jpeg" class="img-fluid rounded shadow-sm" />
      </div>

      <div class="col-6 col-md-4">
        <img src="/img/couple5.jpg" class="img-fluid rounded shadow-sm" />
      </div>

      <div class="col-6 col-md-4">
        <img src="/img/couple6.jpg" class="img-fluid rounded shadow-sm" />
      </div>

    </div>
  </div>

  <!-- JUMBOTRON -->
  <div class="container">
    <div class="p-5 mb-4 bg-white rounded-3 shadow-sm">
      <div class="container-fluid py-3">

        <h1 class="display-6 fw-bold text-center">Verify Your Entry Code</h1>
        <p class="text-center fs-5">
          Enter the 4-digit code sent to you after registration.
        </p>

      </div>
    </div>
  </div>

  <!-- FLASH MESSAGE (if any) -->
  <div class="container">
    <% if (message) { %>
      <div class="alert alert-info text-center"><%= message %></div>
    <% } %>
  </div>

  <!-- SIMPLE BOOTSTRAP VERIFY FORM -->
  <div class="container mb-5">
    <div class="card shadow-sm p-4 mx-auto" style="max-width: 500px;">

      <h4 class="mb-3 text-center">Enter Code</h4>

      <form action="/verify" method="POST" class="text-center">

        <input
          type="text"
          name="code"
          maxlength="4"
          class="form-control mb-3 py-2 text-center"
          placeholder="Enter 4-digit Code"
          required
        />

        <button type="submit" class="btn btn-primary w-100 py-2">
          Verify
        </button>

      </form>

    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
